function s(p0, p1, p2)
{
  parent.location = 'm' + 'ailto:' + p1 + '@' + p0 + '.' + p2;
  return true;
}

function ms(p0, p1, p2)
{
  window.status = 'm'+'ail'+'to:' + p1 + '@' + p0 + '.' + p2;
  return true;
}

